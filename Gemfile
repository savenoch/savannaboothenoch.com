source "https://rubygems.org"

# Mirrors the exact gem versions GitHub Pages builds with, so a local
# `bundle exec jekyll serve` matches production.
# Docs: https://pages.github.com/versions/
gem "github-pages", group: :jekyll_plugins

# Faster file-watching on macOS during local development.
gem "webrick", "~> 1.8"
