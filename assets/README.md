# assets/

Drop your downloadable resume here as **`resume.pdf`**.

The "Download PDF" button on the site links to `/assets/resume.pdf`.
Until you add that file the button will 404 — if you'd rather hide the
button for now, set `resume_pdf: false` in `_data/resume.yml`.
